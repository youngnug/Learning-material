#lul

def function(a, b):
    if a != 'dropout':
        return (f'ERROR: {a} is an absolute savage')
    elif a == 'dropout':
        return(f'{a} is {b} af boiiiii')


a = str(input('Enter first name: '))
b = str(input('Enter attributte: '))


print(function(a,b)+ '\n')

x = input('Press Enter to try again: ')
for stuff in x:
    if x == '':
        pass 

a = str(input('Enter first name: '))
b = str(input('Enter attributte: '))

print(function(a,b))
